rootProject.name = 'config-server'
dependencyResolutionManagement {
    versionCatalogs {
        create("libs") {
            // Pointing Gradle to a custom file location
            from(files("../gradle/libs.versions.toml"))
        }
    }
}