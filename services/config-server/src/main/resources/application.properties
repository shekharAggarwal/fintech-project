server.port=8888
spring.profiles.active=native
spring.application.name=config-server

# Use environment variable for config repo location, fallback to local path
spring.cloud.config.server.native.search-locations=${CONFIG_REPO_PATH:C:/spring boot/fintech-project/services/config-repo}

